<% style = '<link rel="stylesheet" href="/assets/css/product.css"> <link rel="stylesheet" href="/assets/css/index.css">' %>

    <main class="producto-detalle">
        
        <a href="/">↩ Volver al Inicio</a>

        <div class="producto-detalle-card">

            <!-- Imagen -->
            <div class="producto-detalle-imagen">

                <img
                  src="<%= product.image || '/assets/imagenes/default.webp' %>"
                  alt="<%= product.name %>"
                >

            </div>

            <!-- Info -->
            <div class="producto-detalle-info">

                <span class="producto-categoria">
                  <%= product.category %>
                </span>

                <h1>
                  <%= product.name %>
                </h1>

                <p class="producto-descripcion">
                  <%= product.description %>
                </p>

                <div class="producto-puntos">

                    <span class="cantidad-puntos">
                      <%= product.points %>
                    </span>

                    <span class="puntos">
                      PUNTOS
                    </span>

                </div>

                <% if (product.stock === 0) { %>
                  <button class="boton-agregar" disabled>
                    <i class="fa-solid fa-ban"></i>
                    Sin stock
                  </button>
                <% } else { %>
                  <form action="/cart/add/<%= product.id %>" method="POST">
                    <button type="submit" class="boton-agregar">
                      <i class="fa-solid fa-cart-plus"></i>
                      Agregar al carrito
                    </button>
                  </form>
                <% } %>

            </div>

        </div>


        <!-- Productos relacionados -->

        <section class="relacionados">

          <h2>
            Productos relacionados
          </h2>

          <% if (relatedProducts.length > 0) { %>

            <section class="productos-grid">

              <% relatedProducts.forEach(product => { %>

                <%- include(
                  '../partials/productCard',
                  { product }
                ) %>

              <% }) %>

            </section>

          <% } else { %>

            <p>
              No hay productos relacionados disponibles.
            </p>

          <% } %>

        </section>

    </main>