<% style = '<link rel="stylesheet" href="/assets/css/index.css">' %>
<% style = '<link rel="stylesheet" href="/assets/css/productCard.css">' %>

  <main>

    <h1 class="titulo-productos">
      Todos los productos
    </h1>

    <section class="productos-grid">

      <% products.forEach(product => { %>

        <%- include('../partials/productCard', { product }) %>

      <% }) %>

    </section>

  </main>